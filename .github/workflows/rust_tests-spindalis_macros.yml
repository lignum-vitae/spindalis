name: Build and test Spinalis Macros

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      working-directory: ./spindalis_macros
      run: cargo build --verbose
    - name: Run tests
      working-directory: ./spindalis_macros
      run: cargo test --verbose
